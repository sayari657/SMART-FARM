# DeepForest microservice — isolated venv (Python 3.10 recommended).
# Install:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
deepforest>=2.1.0
fastapi
uvicorn[standard]
python-multipart
