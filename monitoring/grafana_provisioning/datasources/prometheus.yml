apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    url: http://prometheus:9090   # Docker service name — resolves inside farm_net
    access: proxy
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
